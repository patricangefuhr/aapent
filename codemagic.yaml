# Codemagic — bygg Åpent (iOS) og last opp til TestFlight.
# Web-app (public/) pakkes i et Capacitor/WKWebView-skall og bygges som .ipa.
workflows:
  ios-apent:
    name: Åpent – iOS TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      # Navn på App Store Connect-nøkkelen i Codemagic (eksakt, kopiert fra integrasjonen).
      app_store_connect: "Åpent"
    environment:
      vars:
        XCODE_PROJECT: "ios/App/App.xcodeproj"
        XCODE_SCHEME: "App"
        BUNDLE_ID: "no.apent.app"
      node: 22
      xcode: latest
    scripts:
      - name: Installer avhengigheter
        script: npm install --no-audit --no-fund
      - name: Bygg web-assets + synk til iOS
        script: |
          npm run build:web
          npx cap copy ios
      - name: Sett opp signering (opprett cert + profil)
        script: |
          keychain initialize
          # CI mangler privat nøkkel til eksisterende cert -> lag et nytt distribusjonssertifikat
          # med fersk privat nøkkel, så vi kan signere og lage profil.
          app-store-connect create-certificate --type IOS_DISTRIBUTION --save
          keychain add-certificates
          # Opprett/hent app_store provisioning-profil for bundle id og sett den på prosjektet.
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create
          keychain add-certificates
          xcode-project use-profiles
      - name: Bygg .ipa
        script: |
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        # Interne testere kan installere med en gang builden er prosessert (ingen review).
